# Database Connection
# Choose one of these options:

# Option 1: Neon Database (Recommended - Free cloud PostgreSQL)
# Sign up at https://neon.tech and copy your connection string
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Option 2: Local PostgreSQL
# DATABASE_URL="postgresql://postgres:password@localhost:5432/planflow"

# Option 3: Docker PostgreSQL
# DATABASE_URL="postgresql://postgres:password@localhost:5432/planflow"

# JWT Secret Key (generate random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SECRET_KEY="your-super-secret-jwt-key-change-this-min-32-chars"

# Email Configuration (Gmail App Password required)
# How to get: Gmail  Settings  Security  2-Step Verification  App passwords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-digit-app-password

# App URL (Frontend URL)
APP_URL=http://localhost:3000

# Server Port
PORT=8000

# Groq API Key for AI Chatbot
# Get free key at: https://console.groq.com
GROQ_API_KEY=your-groq-api-key-starting-with-gsk

# Notes:
# - Never commit .env to git (it's in .gitignore)
# - Each developer needs their own .env file with their own credentials
# - Neon database is recommended for beginners (no installation needed)
